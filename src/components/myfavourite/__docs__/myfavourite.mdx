import { Canvas, Meta } from "@storybook/blocks";
import Example from "./example.tsx";
import * as MyFavourite from "./myfavourite.stories";

<Meta of={MyFavourite} title="MyFavourite" />

# MyFavourite

Icono que muestra un icono de favorito (un corazón) y permite personalizar su color y funcionalidad.

#### Example: Default Favourite

<Canvas of={MyFavourite.Primary} />

## Usage

```ts
import MyFavourite from 'milibreria';

function Example() {
  return (
    <MyFavourite
      color="red"
      onClick={() => alert("MyFavourite Clicked")}
      disabled={false}
    />
  );
}

export default Example;
